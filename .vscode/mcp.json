{
	"servers": {
		"symdex": {
			"type": "stdio",
			"command": "/users/samroberts/.symdex/symdex",
			"args": [
				"serve-mcp",
				"--watch",
				"${workspaceFolder}"
			],
			"cwd": "${workspaceFolder}"
		}
	},
	"inputs": []
}